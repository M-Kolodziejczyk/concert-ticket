import React from "react";

import "./success-message.styles.scss";

const MessageSuccess = ({ children }) => (
  <label className="message-success">{children}</label>
);

export default MessageSuccess;
