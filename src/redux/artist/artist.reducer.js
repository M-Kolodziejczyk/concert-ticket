// import ActionArtistTypes from "./artist.types";

const INITIAL_STATE = {
  ownerArtist: {},
};

const artistReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default artistReducer;
